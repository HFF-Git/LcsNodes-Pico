# DCC Packet Formatter Library

This library implements the routines for formatting a DCC packet in human readable form. The interface accepts the packet data and produces a string with the decoded content.