# DCC Packet Formatter Library

This library implements the routines for formatting a DCC packet in human readable form. The interface accepts the püacket data and produces a string with the decoded content.